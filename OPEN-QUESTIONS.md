# OPEN QUESTIONS — Mizan Inventory System

> Genuine ambiguities and decisions awaiting confirmation. Ref: `CLAUDE.md` §0, §18.
> When blocked, take the more conservative option, record it here, and keep moving.

| # | Question | Conservative choice taken (for now) | Needs confirmation from |
|---|----------|-------------------------------------|-------------------------|
| 1 | Exact rack/level counts per store room (spec says "~6 racks", "~5 levels" — "confirm in Phase 1"). | Seed with 2 rooms × 6 racks × 5 levels = 60 nodes, per spec §5. | Each deployment's customer |
| 2 | Product name shown as "Mizan" — is this the customer-facing product brand, or does each customer want their own name on it? | Use **Mizan** as the product/platform brand; each deployment's organisation is the tenant, white-labelled via Organization Settings. | You / client |
| 3 | Database naming: spec §16 example uses `dcaa`/`dcaa_inventory`; product-neutral is `mizan`/`mizan_inventory`. | Used `mizan` / `mizan_inventory` (product-level). Purely infra config, easily changed. | — (low impact) |
| 4 | SMTP relay host + credentials for supplier emails. | Left blank in `.env.example`; wired but inert until provided. | Each deployment's IT |
| 5 | Which officer/role receives the daily replenishment notification, and by what channel (email vs in-app)? | Assume in-app notification to ADMIN for MVP; email optional later. | Each deployment's customer |
| 6 | Prisma schema location — spec §3 places it at `database/schema/`, but that caused a monorepo module-resolution bug (Prisma kept auto-installing a stray `@prisma/client` at the repo root, breaking the seed). | **Moved Prisma (schema, migrations, seed) to `backend/prisma/`** — the standard layout, where the only consumer (the backend) resolves the client cleanly. `database/schema/README.md` points to the new location; the append-only SQL stays in `database/migrations/`. Low risk; documented deviation from §3. | — (engineering decision) |

| 7 | Where should the reorder engine live? Spec §3.3 places `reorder_calculator` + `reasoning_generator` in the Python `ai-service`. | Implemented the **deterministic** reorder calculator + bilingual reasoning **in the backend** (`modules/replenishment`) — it is transparent template maths (§8.2/§8.3), identical in any language, and runs/verifies with no GPU or Ollama. The Python `ai-service` keeps the spec structure and owns the **scheduled trigger** + the Stage-2 assistant/forecasting (which genuinely need the LLM). Same logic, verifiable today; low risk. | — (engineering decision) |

| 8 | `qwen-production` model tag — spec named `qwen2.5:14b-instruct-q4_K_M`, but the actual Jetson Orin Nano is an 8GB unified-memory board. | Tested directly on the box under real concurrent load (backend + frontend + Postgres + Ollama all running). 14B never fit; 7B previously failed; default-quant `qwen2.5:3b` worked but left only ~100-250MB free and crashed once (CUDA OOM) under real startup load; quantizing 3B further made things worse, not better — `q2_K` produced gibberish and `q3_K_M` hallucinated a wrong stock total in Arabic (violates §8.4's "never invent numbers"). **Chose `qwen2.5:1.5b`**: comfortable 1.3-2.7GB headroom, numbers always correct, but Arabic is noticeably terser/less fluent than 3B. `arabic_verified=False` until the §2.2 gate is actually run against it — see `ai-service/src/language_model/model_profiles.py`. **Update, same day**: `qwen2.5:1.5b` also hit the CUDA allocator error once, on a cold load after Ollama's idle-unload — so this is a board/driver-level flake independent of model size, not something choosing a smaller model eliminates. An immediate retry always succeeded in testing, so `OllamaProvider.complete()` now retries once automatically (`ollama_provider.py`) rather than surfacing a degraded answer for a transient, non-capacity error. | User confirmed (2026-09-02) |
| 9 | Assistant intent routing — stack table (§4) lists `sentence-transformers` + FAISS. | With ~50 products and a handful of fixed intents, skipped **both**: FAISS is unjustified overhead at this catalogue size, and M1's testing showed this 8GB board is already memory-marginal with just Ollama + the app stack loaded — adding a second resident ML model (a multilingual sentence-transformer, ~400-500MB) for fuzzy product-name matching would reintroduce the same OOM risk the model-choice decision (#8) was made to avoid. Product-name matching uses plain keyword/substring matching plus `difflib` fuzzy matching (Python stdlib, no extra memory) instead — deterministic, and arguably more in the spirit of §18's "deterministic over generative" anyway. Both `faiss-cpu` and `sentence-transformers` stay commented out in `ai-service/requirements.txt`. | — (engineering decision, revisit if catalogue size or hardware headroom grows) |

| 10 | Assistant generality — the original design was a fixed menu of ~7 hand-written keyword intents (§8.4 taken literally). Live feedback: "should I reorder Bottled Water 1.5L (6s)?" and "which dairy and creamers category products are about to finish?" both failed — no phrasing/filter combination outside the menu had any path to real data. | Added Ollama tool-calling as a second, general tier: `qwen2.5:1.5b` picks from 6 tools mirroring the same real backend queries (confirmed capable of correct tool + argument selection with an explicit "always call a tool" system prompt). **Priority order matters**: keyword router first (precise, tested), tool-calling only for what it can't express — tool-calling-first caused a real regression (picked a less-useful tool for a case the keyword router already handled exactly). Tool-calling gets one retry (the small model doesn't reliably produce a tool call on the first attempt, especially in Arabic). | — (engineering decision, validated live) |
| 11 | Found extending #10: in Arabic, when both the keyword router and tool-calling fail to resolve a question, the general-chat fallback **fabricated fake product sub-category names** (not in the catalogue) with no number in the answer for the existing verification to catch — a correctness violation beyond the already-documented fluency weaknesses. English handled the identical scenario correctly in the same test. | Arabic general-chat now skips LLM generation entirely in this tier and returns a fixed safe message — deterministic, not a prompt tweak, since prompting alone didn't prevent it even after a retry. Real UX cost (genuine Arabic small talk gets the same message as an unresolved query) accepted deliberately over fabrication risk. See `language-model-validation.md`. | — (engineering decision, safety-motivated) |

| 12 | Found live, in the actual browser panel (7 rephrasings of "which product is critical" all failed with "I couldn't find a clear match"). Two causes: "critical" wasn't a recognised keyword anywhere, and the model reliably calls a tool for imperative phrasing ("list critical products") but not interrogative phrasing ("which product is critical") — confirmed directly against Ollama. | Added "critical"/"out of stock" (EN+AR) to the keyword router directly rather than relying on tool-calling for such a common word — deterministic, instant, works regardless of phrasing style. Also added `detect_severity_filter()` so a "critical" question returns only the critical item(s), not the whole low+critical+out list, and `clean_search_text()` to drop generic status words (e.g. `search_text: "critical"`) a tool call sometimes echoes back, which would otherwise silently zero out real results. All 7 originally-failing rephrasings re-verified live, all correctly return the one real critical product (Floor Cleaner 5L, 5 units). | — (engineering decision, validated live) |

_No blocking questions at this stage._
